# Program-Write a Python script that checks if Python is installed on the system and outputs the version number.

import sys

# Check if Python is installed and retrieve the version
python_version = sys.version
print(f"Python is installed. Version: {python_version}")
